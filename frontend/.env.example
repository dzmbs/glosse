# URL of the FastAPI backend, used for SSR/server-component fetches.
# Client-side calls use the relative /api/* path and go through the
# next.config.ts rewrite.
INTERNAL_API_BASE=http://127.0.0.1:8123
