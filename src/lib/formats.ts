// Book formats foliate-js can render. Used by the file picker, the drag
// filter, and the filename → title fallback in ingest.
export const SUPPORTED_EXT_REGEX = /\.(epub|mobi|azw3|fb2|cbz|pdf)$/i;

export const SUPPORTED_ACCEPT =
  ".epub,.mobi,.azw3,.fb2,.cbz,.pdf,application/epub+zip";
